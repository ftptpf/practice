package ru.practice.job4j.sort;

import java.util.Arrays;

/**
 * Метод сортирует по возрастанию переданный массив и возвращает его.
 */
public class ArraysSort {
    public static long[] sort(long[] data) {
        Arrays.sort(data);
        return data;
    }
}
